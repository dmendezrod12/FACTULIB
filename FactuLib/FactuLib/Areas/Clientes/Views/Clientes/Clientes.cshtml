@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model DataPaginador<InputModelRegistrar>
@{
    ViewData["Title"] = "Customers";
}

<div class="container p-4">
    <div class="row">
        <div class="form-group">
            <form asp-area="Clientes" asp-controller="Clientes" asp-action="Clientes" method="get">
                <div class="row">
                    <div class="form-group">
                        <div class="col-md-12">
                            <input type="text" id="filtrar" name="Filtrar" placeholder="Buscar" class="form-control" />
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="col-md-4">
                            <input type="submit" value="Buscar" class="btn btn-success" />
                        </div>
                    </div>
                </div>
            </form>
        </div>
    </div>
    <div class="row">
        @foreach (var item in Model.List)
        {
            var image = "data:image/jpg;base64," + Convert.ToBase64String(item.Image, 0, item.Image.Length);
            <div class="col-xs-6 col-md-2">
                <div class="card text-center">
                    <div class="card-header">
                        <a asp-route-id="@item.Cedula" asp-area="Clientes" asp-page="/Account/Details">
                            <img class="imageUser" src="@image" />
                        </a>
                    </div>
                    <div class="card-body text-center">
                        <div class="col-md-10">
                            <div class="row">
                                <p>@Html.DisplayFor(modelItem => item.Name)</p>
                            </div>
                            <div class="row">
                                <p>@Html.DisplayFor(modelItem => item.Apellido1)</p>
                            </div>
                            <div class="row">
                                <p>@Html.DisplayFor(modelItem => item.Apellido2)</p>
                            </div>
                            <div class="row">
                                <a class="btn btn-success" asp-route-id="@item.Cedula" asp-area="Clientes" asp-page="/Account/Details"> Detalles </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
<div class="text-center">
    Resultados @Html.Raw(Model.Pagi_info)
    <br />
    @Html.Raw(Model.Pagi_navegacion)
</div>
